import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFFF97316);
  static const Color secondary = Color(0xFFFB923C);
  static const Color accent = Color(0xFFFDBA74);
  static const Color background = Color(0xFFFBFBFB);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color card = Color(0xFFFFFFFF);
  static const Color textPrimary = Color(0xFF1E293B);
  static const Color textLight = Color(0xFF64748B);
  static const Color border = Color(0xFFE5E7EB);
  static const Color error = Color(0xFFEF4444);
  static const Color black = Color(0xFF000000);
  
  static const Color lightBlue = Color(0xFF38BDF8); // Light blue
  static const Color lightBlueBg = Color(0xFFE0F2FE); // Light blue background
  static const Color lightGreen = Color(0xFF4ADE80); // Light green
  static const Color lightGreenBg = Color(0xFFDCFCE7); // Light green background
}

class AppConstants {
  // Spacing
  static const double screenPadding = 24.0;
  static const double cardPadding = 20.0;
  static const double sectionGap = 24.0;
  static const double componentGap = 16.0;
  static const double elementGap = 8.0;

  // Border Radius
  static const double buttonRadius = 16.0;
  static const double cardRadius = 24.0;
  static const double inputRadius = 16.0;
  static const double bottomSheetRadius = 32.0;
  static const double dialogRadius = 28.0;
  static const double imageRadius = 20.0;

  static const Duration animationDuration = Duration(milliseconds: 300);
}
